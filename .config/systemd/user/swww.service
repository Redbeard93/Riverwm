[Unit]
Description=swww daemon
[Service]
ExecStartPre=/bin/sh -c 'while [ ! -S "$XDG_RUNTIME_DIR/$WAYLAND_DISPLAY" ]; do sleep 0.2; done'
ExecStart=/usr/bin/swww-daemon
Restart=on-failure
RestartSec=1
TimeoutStartSec=1

[Install]
WantedBy=default.target
